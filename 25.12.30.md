# 赵谦评议中值得关注的点
## 1 图像噪声生成及去噪
**对抗网络**（Generative Adversarial Networks）
分为生成器generator/判别器discriminator
在图像噪声中，如高斯的普通噪声太规律。GAN可以学习现实相机传感器产生的复杂不规则噪声，从而产生更真实的训练数据。

初始GAN使用JS散度，但$P_real$与$P_generated$所在的流形互不重叠。
新：Wasserstein距离，能够产生一个梯度。

$$W(P_r, P_g) = \inf_{\gamma} \mathbb{E}_{(x,y) \sim \gamma} [ \| x - y \| ]$$

通过对偶变换：(WGAN)

$$W(P_r, P_g) = \sup_{\|f\|_L \le 1} [ \mathbb{E}_{x \sim P_r}[f(x)] - \mathbb{E}_{x \sim P_g}[f(x)] ]$$

$f$ 必须是 1-Lipschitz 的。
通俗地说，这要求 $f$ 这个函数的斜率（导数）不能超过 1。

**对偶对抗** （DualGAN）
为了解决没有配对数据的问题。
同一场景、同一时刻拍摄的“含噪图”和“纯净图”作为完美的一对训练数据。
Dual结构包含两个GAN组成的闭环：
1. 正向映射：把含噪图像变成去噪图
2. 反向映射：把去噪图变成含噪图
**关键机制——循环一致性 (Cycle Consistency)** 一张图去噪后再加噪，应该和原图一样。这限制了网络不能随意乱改图像内容，只能改变噪声特征。

## 2 基于隐空间编码的模糊核生成与去模糊
**模糊核**
在数学上，模糊核（Blur Kernel）通常被记作 $k$。一张模糊图像 $y$ 的形成过程，通常被建模为一张清晰图像 $x$ 与模糊核 $k$ 的卷积过程，再加上一些噪声 $n$。
$$y = x * k + n$$

模糊核 $k$ 本质上就是一个小的矩阵（权重表），它记录了成像瞬间，光线是如何发生畸变和扩散的轨迹。

结合隐空间编码：
1. 学习流形：先用大量真实的模糊核（轨迹、光斑）训练一个网络（如 VAE 或 GAN），学习模糊核的隐空间分布。
2. 隐码生成：去模糊网络不再预测 $k$ 这个矩阵，而是预测一个低维向量 $z$（隐码）。
3. 解码：通过解码器将 $z$ 映射回 $k$。